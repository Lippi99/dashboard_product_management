import { styled } from "../../stitches.config";

export const Container = styled("main", {
  marginLeft: "15rem",
  marginTop: "5rem",
  position: "absolute",
  flex: 1,
  top: 0,
  right: 0,
  left: 0,
  zIndex: -1,
  background: "$background",
});
