import { styled } from "../../stitches.config";

export const Container = styled("main", {
  marginLeft: "15rem",
  marginTop: "5rem",
  position: "absolute",
  top: 0,
  right: 0,
  bottom: 0,
  left: 0,
  zIndex: -1,
  background: "$background",
  overflow: "hidden",
});
